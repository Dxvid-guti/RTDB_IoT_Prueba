import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RTDB_IoT_Prueba',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
